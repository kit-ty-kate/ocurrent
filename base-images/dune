(executables
  (names base_images)
  (libraries current
             current_web
             current_docker
             current_git
             dockerfile-opam
             lwt.unix
             logs.fmt
             fmt.tty))
